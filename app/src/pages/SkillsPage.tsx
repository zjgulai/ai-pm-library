import { Terminal } from 'lucide-react'
import CategoryPage from '@/components/CategoryPage'

export default function SkillsPage() {
  return <CategoryPage category="skill" categoryLabel="技能" icon={Terminal} />
}
